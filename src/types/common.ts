export type Todo = {
  id: number;
  title: string;
  description: string;
  checked: boolean;
};

export type Project = {
  id: number;
  name: string;
};

export type Product = {
  id: number;
  name: string;
};
